# Configure timeout to choose the kernel
#TIMEOUT="3"

# Configure default kernel to boot: check all kernels in `/boot/extlinux/extlinux.conf`
#DEFAULT=""

# Configure additional kernel configuration options in addition to `/etc/kernel/cmdline`
#APPEND=""

# Configure Device Tree overlays
# It is ok to include non existing overlays here, as U-Boot will skip those
FDTOVERLAYS=("rockchip/overlay/rk3568-fiq-debugger-uart2m0.dtbo")
