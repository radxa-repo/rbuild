# settings for r8125-dkms

# map the specific PCI IDs instead of blacklisting the whole pgdrv module
alias   pci:v000010ECd00008125sv*sd*bc*sc*i*    r8125

# if the aliases above do not work, uncomment the following line
# to blacklist the whole pgdrv module
#blacklist	pgdrv
